import styled from "styled-components";

export const StyledLogo = styled.div`
  flex-shrink: 0;
  pointer-events: all;
  position: relative;
  z-index: 99;

  height: 100%;
  svg {
    width: auto;
    height: 100%;
  }
`;
